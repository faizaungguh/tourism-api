import path from 'path';
import fs from 'fs/promises';

export const destination = {
  photoMedia: async () => {},

  addGallery: async () => {},

  patchGallery: async () => {},

  dropGallery: async () => {},
};
